# g-strings
Text fixes for GAMMA (0.9.4+), using XML overrides instead of direct file replacements

## General style rules
- Only overwrite specific nodes that need to be overwritten
- TBD: capitalization rules (always capitalize, never capitalize, sentence case)
- TBD: First-person versus third-person PDA messages
